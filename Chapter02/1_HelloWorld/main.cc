#include <iostream>

// C++: g++ (gcc) Compiler for Windows and Linux
int main()
{
    std::cout << "Hello World!" << std::endl;

    return 0;
}

// Creat exe. --> g++ main.cc -o main
//      with Compiler  sourcefile -o targetfile

// Execute exe. --> ./targetfile (LINUX)
// Execute exe. --> targetfile.exe (WINDOWS)
